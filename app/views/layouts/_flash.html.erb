<% flash.each do |type, message| %>
  <% next if message.blank? %>
  <div class="mx-auto mt-4 max-w-2xl px-4">
    <p class="rounded-md px-4 py-2 text-sm <%= type.to_s == "alert" ? "bg-red-50 text-red-800" : "bg-green-50 text-green-800" %>">
      <%= message %>
    </p>
  </div>
<% end %>
